class P
{
    void Main()
    {
        int a, b, resultado;

        /* --- Entrada --- */
        write("Ingrese el primer número: ");
        read(a);

        write("Ingrese el segundo número: ");
        read(b);

        /* --- Proceso --- */
        resultado = a + b;

        /* --- Salida --- */
        write("La suma es: ");
        write(resultado);
    }
}
